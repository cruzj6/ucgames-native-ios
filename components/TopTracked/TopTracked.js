import React, { Component } from 'react';
import TopTrackedList from './TopTrackedList';
import {
  StyleSheet,
  View,
} from 'react-native';

const TopTracked = () => (
  <View>
    <TopTrackedList />
  </View>
);

export default TopTracked;
