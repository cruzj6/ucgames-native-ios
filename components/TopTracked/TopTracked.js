import React from 'react';
import {
  View,
} from 'react-native';
import TopTrackedList from './TopTrackedList';

const TopTracked = () => (
  <View>
    <TopTrackedList />
  </View>
);

export default TopTracked;
